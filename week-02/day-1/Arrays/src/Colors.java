public class Colors {
    public static void main(String[] args) {
        String[][]colors = {{"lime", "forest green", "olive", "pale green", "spring green"},
                            {"orange red", "red", "tomato"},
                            {"orchid", "violet", "pink", "hot pink"}};
        /*
        prints specific element from specific array.
        System.out.println(colors[0][2]);
        System.out.println("----------------");
        // prints in diagnol. 1 element from each array.
        for ( int elements =0; elements < 3; elements++)
            System.out.println(colors[elements][elements]);
        System.out.println("----------------");
        //Prints all the elements in the arrays.
        for ( int array = 0; array < 3; array++) {
            for (int element = 0; element < 3; element++){
                System.out.println(colors[array][element]);
            }
        }
        */

    }
}
// - Create a two dimensional array
//   which can contain the different shades of specified colors
// - In `colors[0]` store the shades of green:
//   `"lime", "forest green", "olive", "pale green", "spring green"`
// - In `colors[1]` store the shades of red:
//   `"orange red", "red", "tomato"`
// - In `colors[2]` store the shades of pink:
//   `"orchid", "violet", "pink", "hot pink"`